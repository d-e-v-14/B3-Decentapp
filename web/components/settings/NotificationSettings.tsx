"use client";

export default function NotificationSettings() {
  return (
    <section>
      <h3 className="text-lg font-semibold mb-4">
        Notifications
      </h3>

      <p className="text-sm text-gray-400">
        Notification preferences will be configurable per device
        once wallet-based identity is enabled.
      </p>
    </section>
  );
}
